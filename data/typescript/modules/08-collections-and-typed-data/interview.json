{
  "moduleId": "typescript-08-collections-and-typed-data",
  "title": "Collections and typed data structures interview drills",
  "items": [
    {
      "question": "You see a production bug involving Array types, tuple types, and readonly tuples — walk through how you would isolate root cause in DevTools or logs.",
      "answer": "Open with mechanism: Array types, tuple types, and readonly tuples covers built-in data structures and iteration protocols. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for array-and-tuple-types?"
      ],
      "sixtySeconds": "60s: Array types, tuple types, and readonly tuples covers built-in data structures and iteration protocols."
    },
    {
      "question": "How does Typing Map, Set, and Weak collections interact with async boundaries, null safety, or memory retention?",
      "answer": "Open with mechanism: Map<K,V> and Set<T> are ES2015 collections with generic type parameters — unlike plain objects, they accept any key type and preserve insertion order. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for map-set-typed?"
      ],
      "sixtySeconds": "60s: Map<K,V> and Set<T> are ES2015 collections with generic type parameters — unlike plain objects, they accept any key type and preserve insertion order."
    },
    {
      "question": "How does The satisfies operator and type-preserving literals interact with async boundaries, null safety, or memory retention?",
      "answer": "Open with mechanism: The satisfies operator and type-preserving literals is where JavaScript dynamic typing surprises C# developers — implicit ToNumber, ToString, and truthiness rules that === and O... Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for satisfies-operator?"
      ],
      "sixtySeconds": "60s: The satisfies operator and type-preserving literals is where JavaScript dynamic typing surprises C# developers — implicit ToNumber, ToString, and truthiness rules that === and O..."
    },
    {
      "question": "Implement or sketch ReadonlyArray, ReadonlySet, and immutable views from scratch and explain one edge case interviewers add.",
      "answer": "Open with mechanism: readonly T[] and ReadonlyArray<T> block mutating methods at compile time — but the underlying array is still mutable at runtime if another reference exists. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for readonly-collections?"
      ],
      "sixtySeconds": "60s: readonly T[] and ReadonlyArray<T> block mutating methods at compile time — but the underlying array is still mutable at runtime if another reference exists."
    },
    {
      "question": "Design an API that uses Typing map/filter/reduce callbacks precisely correctly — what validation do you add at the boundary?",
      "answer": "Open with mechanism: Array.map, filter, and reduce infer callback parameter types from the array element type — but generic pipelines, tuple returns, and reduce accumulators need explicit type param... Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for typed-array-methods?"
      ],
      "sixtySeconds": "60s: Array.map, filter, and reduce infer callback parameter types from the array element type — but generic pipelines, tuple returns, and reduce accumulators need explicit type param..."
    },
    {
      "question": "Compare TypeScript collections versus C# generics collections in typescript versus C#: what mental model transfers and what does not?",
      "answer": "Open with mechanism: TypeScript arrays and Map/Set erase at compile time — C# List<T> and Dictionary<K,V> enforce generics at runtime through the CLR. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for collections-vs-csharp?"
      ],
      "sixtySeconds": "60s: TypeScript arrays and Map/Set erase at compile time — C# List<T> and Dictionary<K,V> enforce generics at runtime through the CLR."
    }
  ]
}
