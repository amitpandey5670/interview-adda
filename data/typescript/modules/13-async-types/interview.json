{
  "moduleId": "typescript-13-async-types",
  "title": "Async types: Promise, AsyncIterable, and AbortSignal interview drills",
  "items": [
    {
      "question": "Design an API that uses Promise<T> and async function return types correctly — what validation do you add at the boundary?",
      "answer": "Open with mechanism: Promise<T> and async function return types coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for promise-and-async-return-types?"
      ],
      "sixtySeconds": "60s: Promise<T> and async function return types coordinates non-blocking I/O on a single thread via the event loop."
    },
    {
      "question": "When would you avoid the \"clever\" version of Awaited<T> and unwrapping nested promises in a code review?",
      "answer": "Open with mechanism: Awaited<T> and unwrapping nested promises coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for awaited-and-async-utility?"
      ],
      "sixtySeconds": "60s: Awaited<T> and unwrapping nested promises coordinates non-blocking I/O on a single thread via the event loop."
    },
    {
      "question": "Compare AsyncIterable, AsyncGenerator, and for-await-of typing in typescript versus C#: what mental model transfers and what does not?",
      "answer": "Open with mechanism: AsyncIterable, AsyncGenerator, and for-await-of typing coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for async-iterable-types?"
      ],
      "sixtySeconds": "60s: AsyncIterable, AsyncGenerator, and for-await-of typing coordinates non-blocking I/O on a single thread via the event loop."
    },
    {
      "question": "Compare AbortSignal, AbortController, and typed cancellation in typescript versus C#: what mental model transfers and what does not?",
      "answer": "Open with mechanism: AbortSignal, AbortController, and typed cancellation coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for abort-signal-typing?"
      ],
      "sixtySeconds": "60s: AbortSignal, AbortController, and typed cancellation coordinates non-blocking I/O on a single thread via the event loop."
    },
    {
      "question": "Compare Typing async errors and Promise rejection reasons in typescript versus C#: what mental model transfers and what does not?",
      "answer": "Open with mechanism: Typing async errors and Promise rejection reasons coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for async-error-typing?"
      ],
      "sixtySeconds": "60s: Typing async errors and Promise rejection reasons coordinates non-blocking I/O on a single thread via the event loop."
    },
    {
      "question": "You see a production bug involving TypeScript async types versus C# Task and IAsyncEnumerable — walk through how you would isolate root cause in DevTools or logs.",
      "answer": "Open with mechanism: TypeScript async types versus C# Task and IAsyncEnumerable coordinates non-blocking I/O on a single thread via the event loop. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for async-types-vs-csharp-task?"
      ],
      "sixtySeconds": "60s: TypeScript async types versus C# Task and IAsyncEnumerable coordinates non-blocking I/O on a single thread via the event loop."
    }
  ]
}
