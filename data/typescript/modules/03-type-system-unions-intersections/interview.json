{
  "moduleId": "typescript-03-type-system-unions-intersections",
  "title": "The type system: unions, intersections, and widening interview drills",
  "items": [
    {
      "question": "Compare Union types (A | B) and exhaustiveness in typescript versus C#: what mental model transfers and what does not?",
      "answer": "Open with mechanism: A union type says a value can be one of several shapes — like a function parameter that accepts either a string or a number. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for union-types?"
      ],
      "sixtySeconds": "60s: A union type says a value can be one of several shapes — like a function parameter that accepts either a string or a number."
    },
    {
      "question": "Design an API that uses Intersection types (A & B) and mixing shapes correctly — what validation do you add at the boundary?",
      "answer": "Open with mechanism: An intersection type merges multiple type descriptions into one value that must satisfy all of them at once — like a user who is both an employee and a manager. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for intersection-types?"
      ],
      "sixtySeconds": "60s: An intersection type merges multiple type descriptions into one value that must satisfy all of them at once — like a user who is both an employee and a manager."
    },
    {
      "question": "When would you avoid the \"clever\" version of Type widening, narrowing, and control flow in a code review?",
      "answer": "Open with mechanism: TypeScript starts with a precise type and sometimes broadens it on purpose — or narrows it when your `if` checks prove which branch you are in. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for widening-and-narrowing-basics?"
      ],
      "sixtySeconds": "60s: TypeScript starts with a precise type and sometimes broadens it on purpose — or narrows it when your `if` checks prove which branch you are in."
    },
    {
      "question": "When would you avoid the \"clever\" version of Assignability, subtyping, and excess property checks in a code review?",
      "answer": "Open with mechanism: TypeScript decides whether one type can stand in for another using structural rules — if the shape fits, the assignment works. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for assignability-and-subtyping?"
      ],
      "sixtySeconds": "60s: TypeScript decides whether one type can stand in for another using structural rules — if the shape fits, the assignment works."
    },
    {
      "question": "How does Discriminated unions and tagged unions interact with async boundaries, null safety, or memory retention?",
      "answer": "Open with mechanism: When each variant carries a shared tag field like `kind` or `type`, TypeScript follows that tag and unlocks the right fields automatically. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for discriminated-unions-intro?"
      ],
      "sixtySeconds": "60s: When each variant carries a shared tag field like `kind` or `type`, TypeScript follows that tag and unlocks the right fields automatically."
    },
    {
      "question": "When would you avoid the \"clever\" version of readonly, Readonly<T>, and immutability flags in a code review?",
      "answer": "Open with mechanism: The readonly keyword tells TypeScript that a property or array should not be mutated after creation — a compile-time promise, not a runtime lock. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for readonly-and-immutability?"
      ],
      "sixtySeconds": "60s: The readonly keyword tells TypeScript that a property or array should not be mutated after creation — a compile-time promise, not a runtime lock."
    },
    {
      "question": "How does Index signatures and Record<K, V> interact with async boundaries, null safety, or memory retention?",
      "answer": "Open with mechanism: An index signature types dynamic string keys on an object — like a dictionary of HTTP headers or translation strings. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for index-signatures?"
      ],
      "sixtySeconds": "60s: An index signature types dynamic string keys on an object — like a dictionary of HTTP headers or translation strings."
    },
    {
      "question": "How does TypeScript structural types versus C# nominal types interact with async boundaries, null safety, or memory retention?",
      "answer": "Open with mechanism: TypeScript checks shape, not names — if two types have the same properties, they are compatible. Show a 5–10 line example from the topic page. Name one pitfall (any at JSON boundary, non-exhaustive switch) and how you would prove the fix with a unit test or heap snapshot. Close with one JavaScript difference.",
      "followUps": [
        "What breaks if you skip validation at the JSON boundary?",
        "How does this differ in JavaScript at run time?",
        "What metric or test would prove your fix for type-system-vs-csharp?"
      ],
      "sixtySeconds": "60s: TypeScript checks shape, not names — if two types have the same properties, they are compatible."
    }
  ]
}
