{
  "moduleId": "csharp-15-concurrency-and-threading",
  "title": "Concurrency in one screen",
  "rememberThis": [
    "async/await is for I/O waits (module 13). Task.Run and Parallel are for CPU-bound work.",
    "Never hold lock across Redis, HTTP, SQS, or database await.",
    "ConcurrentDictionary and Channel beat hand-rolled locked List for shared state.",
    "Cache stampede: single-flight per key plus SemaphoreSlim on database — not a global lock."
  ],
  "bullets": [
    "Thread pool starvation comes from blocking .Result or synchronous I/O on pool threads.",
    "Interlocked for counters; lock for multi-step invariants kept microseconds short.",
    "PLINQ only pays off on large sequences with non-trivial per-element work.",
    "Fixed global lock order prevents deadlocks when multiple locks are unavoidable."
  ],
  "rows": [
    { "label": "CPU parallelism", "value": "Task.Run, Parallel.ForEach, PLINQ AsParallel" },
    { "label": "I/O concurrency", "value": "async/await — see module 13" },
    { "label": "Message passing", "value": "System.Threading.Channels with bounded capacity" },
    { "label": "Stampede fix", "value": "ConcurrentDictionary<string, Task<T>> single-flight" }
  ]
}
