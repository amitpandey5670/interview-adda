{
  "id": "csharp-15-thread-pool-task-parallel",
  "slug": "thread-pool-task-parallel",
  "title": "Thread pool, Task Parallel Library (TPL), and PLINQ",
  "moduleId": "csharp-15-concurrency-and-threading",
  "order": 1,
  "hook": "The thread pool reuses operating system threads so you do not pay the cost of creating one per work item. The Task Parallel Library (TPL) schedules CPU-bound work on that pool; Parallel LINQ (PLINQ) splits sequence operations across cores.",
  "sections": [
    {
      "heading": "What is this?",
      "blocks": [
        {
          "type": "prose",
          "text": "ThreadPool.QueueUserWorkItem and Task.Run queue delegates to a shared pool of worker threads. Task represents asynchronous work — often I/O via async/await (module 13) or CPU work via Task.Run. Parallel.For and PLINQ (.AsParallel()) partition data across threads for CPU-bound algorithms. The pool grows under load and shrinks when idle."
        },
        {
          "type": "glossary",
          "title": "Key terms",
          "entries": [
            {
              "term": "Thread pool",
              "longForm": ".NET thread pool",
              "plainDefinition": "A managed pool of worker threads shared by the runtime and your code.",
              "example": "Task.Run schedules work on a pool thread."
            },
            {
              "term": "TPL",
              "shortForm": "TPL",
              "longForm": "Task Parallel Library",
              "plainDefinition": "APIs built around Task for parallel and asynchronous programming.",
              "example": "Task.WhenAll, Parallel.ForEach, and PLINQ are TPL features."
            },
            {
              "term": "PLINQ",
              "shortForm": "PLINQ",
              "longForm": "Parallel LINQ",
              "plainDefinition": "Parallel execution of LINQ to Objects queries via .AsParallel().",
              "example": "numbers.AsParallel().Where(n => IsPrime(n)).ToList()"
            }
          ]
        }
      ]
    },
    {
      "heading": "Why does it matter?",
      "blocks": [
        {
          "type": "prose",
          "text": "Blocking the pool with synchronous I/O starves other work — that is why module 13 uses async for Redis and HTTP. CPU-bound CSV parsing or image resizing belongs on the pool via Task.Run or Parallel, not on the request thread. Misusing PLINQ on tiny collections adds overhead without speedup."
        },
        {
          "type": "callout",
          "variant": "tip",
          "title": "CPU vs I/O split",
          "body": "async/await for I/O waits. Task.Run or Parallel for CPU-bound work that would block a thread for milliseconds of computation."
        }
      ]
    },
    {
      "heading": "How it works step by step",
      "blocks": [
        {
          "type": "steps",
          "title": "Scheduling CPU work",
          "items": [
            {
              "title": "Identify CPU-bound section",
              "body": "Hashing, compression, parsing millions of rows — not network waits."
            },
            {
              "title": "Choose API",
              "body": "Task.Run for a chunk; Parallel.ForEach for data parallelism; PLINQ for LINQ-style queries."
            },
            {
              "title": "Avoid oversubscription",
              "body": "Do not nest Parallel inside Parallel without throttling."
            },
            {
              "title": "Return to caller",
              "body": "await Task.Run(...) keeps the API async-friendly for ASP.NET Core."
            }
          ]
        },
        {
          "type": "diagram",
          "diagram": {
            "type": "mermaid",
            "title": "Thread pool scheduling",
            "source": "flowchart TD\n  A[Work item] --> B[Thread pool queue]\n  B --> C[Worker thread 1]\n  B --> D[Worker thread 2]\n  B --> E[Worker thread N]\n  F[Task.Run CPU work] --> B\n  G[async I/O await] -->|releases thread| B"
          }
        }
      ]
    },
    {
      "heading": "Code walkthrough",
      "blocks": [
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Task.Run for CPU-bound work",
            "code": "public async Task<string> HashFileAsync(string path, CancellationToken ct)\n{\n    return await Task.Run(() =>\n    {\n        using var stream = File.OpenRead(path);\n        using var sha = SHA256.Create();\n        var hash = sha.ComputeHash(stream);\n        return Convert.ToHexString(hash);\n    }, ct);\n}",
            "explanation": "File read is I/O; hashing is CPU. Task.Run moves hashing off the request thread to a pool thread."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Parallel.ForEach for CSV rows",
            "code": "var results = new ConcurrentBag<RowResult>();\nParallel.ForEach(rows, new ParallelOptions { MaxDegreeOfParallelism = 4 }, row =>\n{\n    var parsed = ParseRow(row);\n    results.Add(parsed);\n});\nreturn results.ToList();",
            "explanation": "Partitions row parsing across up to four threads. Use ConcurrentBag for thread-safe collection."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "PLINQ with degree limit",
            "code": "var primes = Enumerable.Range(2, 100_000)\n    .AsParallel()\n    .WithDegreeOfParallelism(Environment.ProcessorCount)\n    .Where(IsPrime)\n    .ToList();",
            "explanation": "PLINQ parallelizes the Where filter. Only worth it for large sequences and non-trivial predicates."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "ReaderWriterLockSlim for read-heavy cache",
            "code": "private readonly ReaderWriterLockSlim _configLock = new();\nprivate Dictionary<string, string> _config = new();\n\npublic string GetSetting(string key)\n{\n    _configLock.EnterReadLock();\n    try { return _config[key]; }\n    finally { _configLock.ExitReadLock(); }\n}\n\npublic void Reload(Dictionary<string, string> next)\n{\n    _configLock.EnterWriteLock();\n    try { _config = next; }\n    finally { _configLock.ExitWriteLock(); }\n}",
            "explanation": "Many concurrent readers share the read lock; reload takes an exclusive write lock. Prefer ConcurrentDictionary when writes are frequent — RW lock wins on rare bulk reloads."
          }
        }
      ]
    },
    {
      "heading": "Compare with JavaScript and TypeScript",
      "blocks": [
        {
          "type": "comparisonTable",
          "title": "Parallelism models",
          "headers": ["Aspect", "C# TPL / PLINQ", "JavaScript"],
          "rows": [
            ["CPU parallelism", "Thread pool, worker threads", "worker_threads module for CPU work"],
            ["I/O concurrency", "async/await on pool threads", "Event loop + non-blocking I/O"],
            ["Data parallel", "Parallel.ForEach, PLINQ", "No built-in PLINQ; manual chunking to workers"],
            ["Combine results", "Task.WhenAll", "Promise.all"]
          ]
        }
      ]
    },
    {
      "heading": "Common mistakes",
      "blocks": [
        {
          "type": "callout",
          "variant": "warning",
          "title": "Pitfalls to avoid",
          "body": "Task.Run for trivial work — overhead exceeds benefit. PLINQ on ten items. Blocking .Result on pool threads causing starvation. Using Parallel when work is I/O-bound — use async instead."
        }
      ]
    },
    {
      "heading": "Senior interview depth",
      "blocks": [
        {
          "type": "prose",
          "text": "How to prove it in production: the thread pool uses hill-climbing to adjust worker count — blocking sync I/O on pool threads raises ThreadPool Queue Length and delays async continuations. Use dotnet-counters (System.Runtime threadpool-queue-length, threadpool-thread-count) during load tests. ReaderWriterLockSlim allows many readers or one writer — good for config snapshots reloaded rarely; ConcurrentDictionary beats it when entries mutate continuously. Task Parallel Library (TPL) work steals through the pool; nested Parallel.ForEach without MaxDegreeOfParallelism can oversubscribe CPU."
        },
        {
          "type": "comparisonTable",
          "title": "Tradeoffs at senior level",
          "headers": ["API", "Pool impact", "When to choose"],
          "rows": [
            ["Task.Run", "Queues CPU work; consumes pool thread", "Offload CPU from ASP.NET request thread"],
            ["Parallel.ForEach", "Partitions work; can oversubscribe", "Large in-memory batches with expensive per-item CPU"],
            ["PLINQ", "Same as Parallel under the hood", "LINQ-style filters on big sequences"],
            ["ReaderWriterLockSlim", "Blocks readers/writers on same lock object", "Rare bulk reload; many concurrent readers"],
            ["ConcurrentDictionary", "Fine-grained internal locking", "Per-key updates; cache maps; single-flight tables"],
            ["async/await I/O", "Releases pool thread during wait", "Redis, HTTP, SQS — not Task.Run wrappers"]
          ]
        },
        {
          "type": "callout",
          "variant": "warning",
          "title": "What gets you rejected in interviews",
          "body": "Task.Run on every controller action 'to be async' — adds pool scheduling without freeing threads during I/O. Using ReaderWriterLockSlim for a hot per-request cache with constant writes — write lock convoys destroy throughput. PLINQ on small collections because 'parallel is faster'. Not knowing pool starvation shows up as rising latency on unrelated endpoints."
        }
      ]
    }
  ],
  "interviewTakeaways": [
    "Thread pool reuses worker threads; Task.Run and Parallel move CPU-bound work off the caller; async/await frees threads during I/O waits.",
    "ReaderWriterLockSlim fits read-heavy, rarely reloaded snapshots; ConcurrentDictionary fits per-key churn — wrong choice causes write-lock convoys.",
    "In production, monitor threadpool-queue-length with dotnet-counters when latency spikes — blocking .Result or sync I/O on pool threads starves continuations.",
    "60s: async for I/O. Task.Run/Parallel/PLINQ for CPU-bound batches. RW lock for rare bulk reload with many readers. Profile pool queue before adding parallelism.",
    "Follow-up: why does nested Parallel.ForEach without MaxDegreeOfParallelism hurt more than help?"
  ],
  "commonPitfalls": [
    "Saying 'Task.Run makes code async' gets you rejected — it queues CPU work; only true async I/O releases the thread during wait.",
    "ReaderWriterLockSlim on a per-request mutable map — writers block all readers constantly.",
    "Strong answer: 'Pool threads are shared. CPU work via Task.Run with cancellation. RW lock only for rare reloads. dotnet-counters for queue length under load.'"
  ],
  "relatedTopicIds": [
    "csharp-13-why-async",
    "csharp-13-promises-vs-tasks",
    "csharp-15-high-concurrency-scenario",
    "csharp-15-concurrent-collections-channels",
    "csharp-13-task-async-await"
  ],
  "jsTsCorrelations": [
    {
      "language": "javascript",
      "concept": "worker_threads",
      "note": "Node worker_threads are the JS analogue for CPU parallelism. The main thread event loop is closer to async I/O in C#.",
      "futureTopicSlug": "javascript/concurrency/worker-threads"
    },
    {
      "language": "typescript",
      "concept": "Promise.all vs pool scheduling",
      "note": "Promise.all composes concurrent I/O on one thread. C# Task.WhenAll also composes waits but pool threads execute continuations — blocking one blocks unrelated work.",
      "futureTopicSlug": "typescript/async/concurrency"
    }
  ],
  "officialSources": [
    {
      "title": "Task-based asynchronous programming",
      "url": "https://learn.microsoft.com/en-us/dotnet/standard/parallel-programming/task-based-asynchronous-programming"
    },
    {
      "title": "Parallel LINQ (PLINQ)",
      "url": "https://learn.microsoft.com/en-us/dotnet/standard/parallel-programming/parallel-linq-plinq"
    },
    {
      "title": "dotnet-counters",
      "url": "https://learn.microsoft.com/en-us/dotnet/core/diagnostics/dotnet-counters"
    }
  ],
  "scenarioTag": "csv-import"
}
