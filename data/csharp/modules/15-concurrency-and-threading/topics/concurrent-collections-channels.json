{
  "id": "csharp-15-concurrent-collections-channels",
  "slug": "concurrent-collections-channels",
  "title": "Concurrent collections and Channels (message passing)",
  "moduleId": "csharp-15-concurrency-and-threading",
  "order": 4,
  "hook": "ConcurrentDictionary and ConcurrentQueue let many threads add and remove without wrapping Dictionary in lock. System.Threading.Channels provide producer-consumer queues with async read and write — a cleaner model than sharing a locked list between RabbitMQ handlers.",
  "sections": [
    {
      "heading": "What is this?",
      "blocks": [
        {
          "type": "prose",
          "text": "Concurrent collections in System.Collections.Concurrent use fine-grained locking or lock-free techniques for thread-safe operations. ConcurrentDictionary<TKey,TValue> supports TryAdd and GetOrAdd atomically. Channel<T> is a bounded or unbounded queue: writers call WriteAsync; readers use await foreach or ReadAsync. Multiple consumers can compete for items safely."
        },
        {
          "type": "glossary",
          "title": "Key terms",
          "entries": [
            {
              "term": "ConcurrentDictionary",
              "longForm": "ConcurrentDictionary class",
              "plainDefinition": "Thread-safe dictionary with atomic add/update operations.",
              "example": "_cache.GetOrAdd(key, k => LoadFromDb(k));"
            },
            {
              "term": "Channel",
              "longForm": "System.Threading.Channels",
              "plainDefinition": "Async producer-consumer queue for passing messages between threads.",
              "example": "Channel.CreateBounded<OrderEvent>(100);"
            },
            {
              "term": "Producer-consumer",
              "longForm": "Producer-consumer pattern",
              "plainDefinition": "Producers enqueue work; consumers dequeue and process independently.",
              "example": "RabbitMQ receiver writes to channel; worker tasks read from channel."
            }
          ]
        }
      ]
    },
    {
      "heading": "Why does it matter?",
      "blocks": [
        {
          "type": "prose",
          "text": "Hand-rolled lock + List<T> for a work queue serializes every enqueue and dequeue. Channels decouple receive thread from processing threads and support backpressure with bounded capacity. ConcurrentDictionary is the right tool for a shared cache map — not Dictionary with lock on every read unless profiling shows you need finer control."
        },
        {
          "type": "callout",
          "variant": "tip",
          "title": "When not to invent",
          "body": "Use BCL concurrent types before building your own lock-free queue. Module 08 covers when custom structures are justified."
        }
      ]
    },
    {
      "heading": "How it works step by step",
      "blocks": [
        {
          "type": "steps",
          "title": "Channel-based pipeline",
          "items": [
            {
              "title": "Create bounded channel",
              "body": "Capacity limits memory when producers outpace consumers."
            },
            {
              "title": "Producer writes",
              "body": "RabbitMQ handler awaits WriteAsync — does not process inline."
            },
            {
              "title": "Start consumer tasks",
              "body": "Multiple readers await ReadAsync in parallel."
            },
            {
              "title": "Complete channel",
              "body": "Writer.Complete() signals no more items; readers drain and finish."
            }
          ]
        },
        {
          "type": "diagram",
          "diagram": {
            "type": "mermaid",
            "title": "Channel pipeline",
            "source": "flowchart LR\n  P1[RabbitMQ receiver] -->|WriteAsync| CH[Channel buffer]\n  CH --> C1[Worker 1]\n  CH --> C2[Worker 2]\n  CH --> C3[Worker N]"
          }
        }
      ]
    },
    {
      "heading": "Code walkthrough",
      "blocks": [
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "ConcurrentDictionary cache",
            "code": "private readonly ConcurrentDictionary<string, UserDto> _users = new();\n\npublic UserDto GetUser(string id) =>\n    _users.GetOrAdd(id, static (key, db) => db.LoadUser(key), _database);",
            "explanation": "GetOrAdd ensures only one factory runs per key even under concurrent requests."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Bounded channel setup",
            "code": "var channel = Channel.CreateBounded<Message>(new BoundedChannelOptions(500)\n{\n    FullMode = BoundedChannelFullMode.Wait\n});\nvar writer = channel.Writer;\nvar reader = channel.Reader;",
            "explanation": "When 500 items are buffered, WriteAsync waits — backpressure instead of unbounded memory growth."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Consumer loop",
            "code": "await foreach (var msg in reader.ReadAllAsync(cancellationToken))\n{\n    await ProcessAsync(msg, cancellationToken);\n}",
            "explanation": "ReadAllAsync yields messages as they arrive. Multiple consumers can each run this loop on the same reader."
          }
        }
      ]
    },
    {
      "heading": "Compare with JavaScript and TypeScript",
      "blocks": [
        {
          "type": "comparisonTable",
          "title": "Queues and shared maps",
          "headers": ["Pattern", "C#", "JavaScript"],
          "rows": [
            ["Thread-safe map", "ConcurrentDictionary", "Map on single thread; no built-in concurrent map"],
            ["Async queue", "Channel<T>", "Async queue libraries or EventEmitter patterns"],
            ["Backpressure", "BoundedChannelFullMode.Wait", "Manual queue length checks"]
          ]
        }
      ]
    },
    {
      "heading": "Common mistakes",
      "blocks": [
        {
          "type": "callout",
          "variant": "warning",
          "title": "Pitfalls to avoid",
          "body": "Unbounded channel when producers can flood memory. Assuming ConcurrentDictionary values are immutable — mutate copies or use locks inside values. Forgetting Writer.Complete() so readers hang forever."
        }
      ]
    }
  ],
  "interviewTakeaways": [
    "ConcurrentDictionary for shared maps; Channels for producer-consumer pipelines.",
    "Bounded channels provide backpressure.",
    "Prefer message passing over shared mutable state when possible."
  ],
  "commonPitfalls": [
    "Unbounded channel in a burst traffic scenario.",
    "Mutating objects stored in ConcurrentDictionary without synchronization.",
    "Single consumer when throughput needs parallel workers."
  ],
  "relatedTopicIds": [
    "csharp-13-async-enumerable",
    "csharp-15-thread-pool-task-parallel",
    "csharp-15-high-concurrency-scenario",
    "csharp-08-when-not-to-invent",
    "csharp-08-list-dictionary-set-queue"
  ],
  "jsTsCorrelations": [
    {
      "language": "javascript",
      "concept": "Async queues",
      "note": "p-queue and similar npm packages mirror bounded worker queues. C# Channels are built into the runtime.",
      "futureTopicSlug": "javascript/concurrency/queues"
    }
  ],
  "officialSources": [
    {
      "title": "Thread-safe collections",
      "url": "https://learn.microsoft.com/en-us/dotnet/standard/collections/thread-safe/"
    },
    {
      "title": "Channels library",
      "url": "https://learn.microsoft.com/en-us/dotnet/core/extensions/channels"
    }
  ],
  "scenarioTag": "rabbitmq"
}
