{
  "moduleId": "csharp-17-span-unsafe-interop",
  "title": "Span and interop in one screen",
  "rememberThis": [
    "Span<T> is a stack-only view — slice without Substring allocations.",
    "Memory<T> is the heap-storable/async-friendly counterpart.",
    "stackalloc only for bounded small buffers — ArrayPool for large CSV chunks.",
    "P/Invoke and SkiaSharp need using — native memory is outside GC."
  ],
  "bullets": [
    "ReadOnlySpan<char> plus TryParse avoids string per CSV field.",
    "fixed pins during native calls — keep blocks short to avoid heap fragmentation.",
    "LibraryImport replaces DllImport for modern trim-safe interop.",
    "Keep unsafe in one reviewed interop project — not spread through domain code."
  ],
  "rows": [
    { "label": "Hot path parse", "value": "line.AsSpan(), IndexOf, TryParse" },
    { "label": "Bounded stack", "value": "stackalloc char[MaxLine] with validated line length" },
    { "label": "Large import", "value": "ArrayPool<byte>, stream, yield return rows" },
    { "label": "Native wrapper", "value": "using SKBitmap / LibraryImport + Dispose" }
  ]
}
