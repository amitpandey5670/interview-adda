{
  "id": "csharp-01-entry-points",
  "slug": "entry-points",
  "title": "Where your program starts (Main and top-level statements)",
  "moduleId": "csharp-01-program-and-compilation",
  "order": 5,
  "hook": "Every executable program needs exactly one starting point. In C# you either write a classic static Main method or put statements at the top of one file and let the compiler build Main for you.",
  "sections": [
    {
      "heading": "What is this?",
      "blocks": [
        {
          "type": "prose",
          "text": "The entry point is the method the operating system calls when your process starts. In classic C# you write static void Main(string[] args) or static int Main to return an exit code. Modern templates use top-level statements: plain executable lines at the top of a single file. The compiler silently generates a Program class and a Main-like method. Both styles produce the same kind of executable — top-level statements are syntactic sugar, not a different runtime."
        },
        {
          "type": "glossary",
          "title": "Key terms",
          "entries": [
            {
              "term": "Main",
              "longForm": "Main method",
              "plainDefinition": "The static method the CLR calls first when your executable starts.",
              "example": "static int Main(string[] args) { ... return 0; }"
            },
            {
              "term": "top-level statements",
              "longForm": "top-level statements",
              "plainDefinition": "Executable C# lines written outside any class; the compiler synthesizes an entry point from them.",
              "example": "Console.WriteLine(\"hello\"); as the entire Program.cs file body."
            },
            {
              "term": "args",
              "longForm": "command-line arguments",
              "plainDefinition": "A string array of values passed to your program when it is launched.",
              "example": "dotnet run -- queue-name passes \"queue-name\" into args[0]."
            }
          ]
        }
      ]
    },
    {
      "heading": "Why does it matter?",
      "blocks": [
        {
          "type": "prose",
          "text": "Only one file per compilation may contain top-level statements. If you copy Program.cs and leave top-level code in both, the build fails. Worker hosts (generic host, AWS Lambda handlers) often hide Main inside the framework — your \"entry\" becomes a handler method, but the process still started at Main under the hood. Understanding args, return codes, and async entry points prevents subtle bugs in command-line tools and services."
        },
        {
          "type": "callout",
          "variant": "remember",
          "title": "One entry per executable",
          "body": "Exactly one entry point per executable compilation. Two files with top-level statements will not compile together."
        }
      ]
    },
    {
      "heading": "How entry points work",
      "blocks": [
        {
          "type": "steps",
          "title": "From dotnet run to your code",
          "items": [
            {
              "title": "OS launches the process",
              "body": "The runtime loader finds the assembly and locates the entry point metadata."
            },
            {
              "title": "CLR calls Main (or synthesized Main)",
              "body": "Top-level statements compile to a hidden static Main method in a generated Program class."
            },
            {
              "title": "args arrives as string[]",
              "body": "Command-line tokens are available in the args parameter or as a top-level args variable."
            },
            {
              "title": "Return code becomes exit code",
              "body": "return 0 from Main (or top-level return 0) tells the OS the program succeeded."
            }
          ]
        },
        {
          "type": "diagram",
          "diagram": {
            "type": "mermaid",
            "title": "Entry point styles",
            "source": "flowchart TD\n  A[Process starts] --> B{Entry style}\n  B -->|Classic| C[static Main string args]\n  B -->|Top-level| D[Compiler-generated Main]\n  C --> E[Your application code]\n  D --> E\n  E --> F[Exit code to OS]"
          }
        }
      ]
    },
    {
      "heading": "Code walkthrough",
      "blocks": [
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Top-level statements (default template)",
            "code": "// Program.cs — top-level style\nvar queue = args.Length > 0 ? args[0] : \"orders\";\nConsole.WriteLine($\"consuming {queue}\");",
            "explanation": "args is in scope automatically. No class or Main wrapper needed. The compiler generates the entry point behind the scenes."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Equivalent explicit Main",
            "code": "namespace Acme;\n\nclass Program\n{\n    static int Main(string[] args)\n    {\n        var queue = args.Length > 0 ? args[0] : \"orders\";\n        Console.WriteLine($\"consuming {queue}\");\n        return 0;\n    }\n}",
            "explanation": "Return codes from Main become the process exit code. Top-level return of an int does the same. Both styles are fully equivalent."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Async entry with await at top level",
            "code": "using var client = new HttpClient();\nvar html = await client.GetStringAsync(\"https://example.com\");\nConsole.WriteLine(html.Length);\n\n// await at top level synthesizes async Task Main instead of void Main.",
            "explanation": "Top-level await changes the generated signature to async Task Main. Useful for console tools that call APIs without blocking."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Exit code from top-level return",
            "code": "if (args.Length == 0)\n{\n    Console.Error.WriteLine(\"usage: tool <queue>\");\n    return 1;\n}\n\nConsole.WriteLine($\"ok {args[0]}\");\nreturn 0; // synthesized Main returns int — process exit code",
            "explanation": "Returning an int from top-level statements sets the process exit code — same as static int Main returning that value."
          }
        }
      ]
    },
    {
      "heading": "Compare with JavaScript and TypeScript",
      "blocks": [
        {
          "type": "comparisonTable",
          "title": "Process entry comparison",
          "headers": ["Aspect", "C# entry point", "Node.js script"],
          "rows": [
            ["How process starts", "CLR calls static Main (or synthesized)", "Node executes the file you pass"],
            ["Arguments", "string[] args", "process.argv (includes node path and script path)"],
            ["Multiple entry files", "Only one top-level file allowed", "One file per node invocation"],
            ["Exit code", "return int from Main", "process.exit(code)"],
            ["Async at start", "await at top level → async Main", "Top-level await in ES modules"]
          ]
        }
      ]
    },
    {
      "heading": "Common mistakes",
      "blocks": [
        {
          "type": "callout",
          "variant": "warning",
          "title": "Pitfalls to avoid",
          "body": "Two Program.cs files both with top-level statements after a copy-paste. Looking for Main in a new template and concluding the app has no entry point. Forgetting that await at top-level changes the synthesized signature to async Main."
        }
      ]
    },
    {
      "heading": "Senior interview depth",
      "blocks": [
        {
          "type": "prose",
          "text": "How to prove it in production: the compiler synthesizes a static Main method from top-level statements — inspect with ildasm or dotPeek to see Program.<Main>$ or similar. Exactly one compilation unit may contain top-level statements per executable project. async Main returns Task; unhandled exceptions propagate differently than void Main. Library projects must not use top-level statements. CI should fail if two files define top-level entry — CS8802."
        },
        {
          "type": "comparisonTable",
          "title": "Tradeoffs at senior level",
          "headers": ["Style", "Generated signature", "When to choose"],
          "rows": [
            ["Top-level statements", "static void/int/Task Main", "Scripts, modern console templates"],
            ["Classic Main", "Explicit static Main", "Libraries teaching, explicit args handling"],
            ["Top-level await", "static async Task Main", "Console tools calling HTTP/Redis"],
            ["return int", "Exit code to OS", "CI scripts checking $?"],
            ["Multiple Main candidates", "CS0017 compile error", "Only one entry per exe"]
          ]
        },
        {
          "type": "callout",
          "variant": "warning",
          "title": "What gets you rejected in interviews",
          "body": "Claiming top-level statements skip Main entirely — compiler generates it. Two files with top-level statements in one project. Not knowing args is string[] without node path prefixes. Using top-level statements in a class library project."
        }
      ]
    }
  ],
  "interviewTakeaways": [
    "Exactly one entry point per executable — top-level statements are syntactic sugar for a compiler-generated static Main.",
    "Top-level await synthesizes async Task Main; returning int sets process exit code for CI scripts.",
    "In production, library projects must use explicit types — no top-level statements; exe projects fail build on duplicate entry files.",
    "60s: One entry per exe. Top-level statements generate Main. args is string[]. return int for exit code. await makes async Main. Libraries cannot use top-level.",
    "Follow-up: how does exception handling differ between async Task Main and void Main?"
  ],
  "commonPitfalls": [
    "Two Program.cs with top-level statements gets you rejected — CS8802 duplicate entry.",
    "Looking for Main in template and concluding no entry point — it is synthesized.",
    "Strong answer: 'Compiler generates Main. One top-level file per exe. async Main for await. return for exit codes. Inspect IL if skeptical.'"
  ],
  "relatedTopicIds": [
    "csharp-01-file-based-apps",
    "csharp-01-namespaces-and-usings",
    "csharp-01-csharp-vs-js-ts"
  ],
  "jsTsCorrelations": [
    {
      "language": "javascript",
      "concept": "process entry and argv",
      "note": "Node runs the file you pass to node. C# always compiles to an entry method. args is argv without the runtime path prefix Node includes.",
      "futureTopicSlug": "javascript/runtime/process-argv"
    },
    {
      "language": "typescript",
      "concept": "Top-level await in ESM",
      "note": "Node ESM allows top-level await like C# top-level await synthesizing async Main — but Node has no single-entry constraint across files in a project.",
      "futureTopicSlug": "typescript/modules/top-level-await"
    }
  ],
  "officialSources": [
    {
      "title": "General structure of a C# program",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/program-structure/"
    },
    {
      "title": "Top-level statements",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/program-structure/top-level-statements"
    }
  ],
  "scenarioTag": "sqs-consumer"
}
