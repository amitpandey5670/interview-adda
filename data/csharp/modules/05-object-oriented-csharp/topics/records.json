{
  "id": "csharp-05-records",
  "slug": "records",
  "title": "Records (value equality and with-expressions)",
  "moduleId": "csharp-05-object-oriented-csharp",
  "order": 5,
  "hook": "A record is a type whose main job is holding data — not behavior or identity. C# generates value equality, a readable `ToString`, and `with`-expressions so you can copy and tweak without mutating the original.",
  "sections": [
    {
      "heading": "What is this?",
      "blocks": [
        {
          "type": "prose",
          "text": "A **record** is a reference type (`record` or `record class`) or value type (`record struct`) optimized for immutable data. Two record instances compare **equal by value** — same type, same member values — even if they are different objects on the heap. Positional syntax (`record AuthToken(string Subject, string Issuer)`) generates a primary constructor, init-only properties, `Deconstruct`, and a formatted `ToString`."
        },
        {
          "type": "glossary",
          "title": "Key terms",
          "entries": [
            {
              "term": "record class",
              "longForm": "record class",
              "plainDefinition": "A reference type with value equality. Lives on the heap but compares by member values, not identity.",
              "example": "`record AuthToken(string Subject, string Issuer);`"
            },
            {
              "term": "record struct",
              "longForm": "record struct",
              "plainDefinition": "A value type with value equality. Copies on assignment like any struct.",
              "example": "`readonly record struct CacheKey(string Tenant, string Id);`"
            },
            {
              "term": "with-expression",
              "longForm": "with-expression",
              "plainDefinition": "Creates a new record instance by copying the original and overlaying named properties. Does not mutate.",
              "example": "`var refreshed = token with { ExpiresAt = newTime };`"
            },
            {
              "term": "positional record",
              "longForm": "positional record",
              "plainDefinition": "A record declared with parameters in parentheses. The compiler generates constructor, properties, equality, and Deconstruct.",
              "example": "`record Point(int X, int Y);`"
            }
          ]
        }
      ]
    },
    {
      "heading": "Why does it matter?",
      "blocks": [
        {
          "type": "prose",
          "text": "Auth tokens, Redis Data Transfer Objects (DTOs), and API response shapes are pure data — they do not need identity or virtual dispatch. Records give you concise syntax and correct equality out of the box. Your SQS worker host, on the other hand, has behavior and lifetime — that stays a class. Using a record where you need a service, or a class where you need value equality, leads to subtle bugs in dictionaries and tests."
        },
        {
          "type": "callout",
          "variant": "remember",
          "title": "with does not mutate",
          "body": "`var refreshed = token with { ExpiresAt = newTime }` creates a new instance. The original `token` is unchanged. This is how you refresh an expiry without touching the copy sitting in a cache."
        }
      ]
    },
    {
      "heading": "How it works step by step",
      "blocks": [
        {
          "type": "steps",
          "title": "Working with records",
          "items": [
            {
              "title": "Declare a positional record",
              "body": "`record AuthToken(string Subject, string Issuer, DateTimeOffset ExpiresAt);` — the compiler generates constructor, init properties, equality, `ToString`, and `Deconstruct`."
            },
            {
              "title": "Create instances",
              "body": "`new AuthToken(\"user-9\", \"https://auth.example\", expiry)` or use the generated constructor."
            },
            {
              "title": "Compare by value",
              "body": "Two records with the same type and same property values return true from `Equals`, even if they are different heap objects."
            },
            {
              "title": "Copy with changes using with",
              "body": "`token with { ExpiresAt = newExpiry }` produces a new record. The original is untouched."
            }
          ]
        },
        {
          "type": "diagram",
          "diagram": {
            "type": "mermaid",
            "title": "record class versus record struct",
            "source": "flowchart TD\n  DATA[Immutable data shape] --> RC[record class\\nheap object\\nvalue equality]\n  DATA --> RS[record struct\\nstack copy\\nvalue equality]\n  RC --> USE1[Auth tokens\\nAPI DTOs\\ntest fixtures]\n  RS --> USE2[Cache keys\\ncoordinates\\nsmall bundles]"
          }
        }
      ]
    },
    {
      "heading": "Code walkthrough",
      "blocks": [
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Auth token and Redis DTO records",
            "code": "record AuthToken(string Subject, string Issuer, DateTimeOffset ExpiresAt)\n{\n    public bool IsExpired(DateTimeOffset now) => now >= ExpiresAt;\n}\n\nreadonly record struct RedisEntry(string Key, int TtlSeconds);\n\nvar token = new AuthToken(\"user-9\", \"https://auth.example\",\n    DateTimeOffset.UtcNow.AddHours(1));\nvar refreshed = token with { ExpiresAt = DateTimeOffset.UtcNow.AddHours(8) };\n\nConsole.WriteLine(token == refreshed);       // False — different ExpiresAt\nConsole.WriteLine(token.Subject == refreshed.Subject); // True",
            "explanation": "Line 1: positional record class with one custom method. Line 6: readonly record struct for cache metadata. Line 10: `with` creates a copy with a new expiry. Lines 12–13: value equality compares all members; `Subject` matches but the records are not equal overall."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Deconstruct and formatted ToString",
            "code": "record GradeEvent(string AttemptId, int Score, string Tenant);\n\nvar evt = new GradeEvent(\"a-42\", 95, \"acme\");\n\n// Generated ToString\nConsole.WriteLine(evt);\n// GradeEvent { AttemptId = a-42, Score = 95, Tenant = acme }\n\n// Generated Deconstruct\nvar (attemptId, score, tenant) = evt;\nConsole.WriteLine($\"{tenant}/{attemptId}: {score}\");",
            "explanation": "Line 1: positional record — compiler generates members. Line 6: `ToString` prints all properties in a readable format. Line 10: `Deconstruct` lets you unpack into variables — useful in pattern matching (module 11)."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "When NOT to use a record",
            "code": "// Entity with identity — use a class\npublic sealed class Attempt\n{\n    public string Id { get; }\n    public int Score { get; set; } // mutable state\n    public Attempt(string id) => Id = id;\n}\n\n// Mutable record — legal but loses dictionary-key safety\npublic record MutableDto\n{\n    public string Key { get; set; } // setter breaks immutability\n}",
            "explanation": "Line 2: `Attempt` has identity (`Id`) and mutable `Score` — a class with reference equality is appropriate. Line 10: a record with `set` accessors is legal but you lose the safe-to-use-as-dictionary-key property. Prefer init-only or positional records for DTOs."
          }
        }
      ]
    },
    {
      "heading": "Compare with JavaScript and TypeScript",
      "blocks": [
        {
          "type": "comparisonTable",
          "title": "Records across languages",
          "headers": ["Concept", "C#", "JavaScript / TypeScript"],
          "rows": [
            ["Copy with changes", "`with { Prop = value }` — nondestructive", "`{ ...obj, prop: value }` — shallow spread"],
            ["Value equality", "Generated `Equals` and `GetHashCode`", "No built-in; `===` is reference equality for objects"],
            ["Immutability", "`init` properties; readonly record struct", "TS `Readonly<T>` is compile-time only"],
            ["ToString", "Generated formatted output", "Default `[object Object]` unless you override"],
            ["Syntax", "`record Name(Type Prop);`", "`interface` or `type` — no runtime record type"]
          ]
        }
      ]
    },
    {
      "heading": "Common mistakes",
      "blocks": [
        {
          "type": "callout",
          "variant": "warning",
          "title": "Using a mutable record as a dictionary key",
          "body": "If any property has a `set` accessor and someone mutates it after inserting into a `Dictionary`, the entry becomes unreachable. Records help only when they stay immutable. Use `init` or positional syntax."
        },
        {
          "type": "callout",
          "variant": "warning",
          "title": "Expecting record struct inheritance",
          "body": "`record struct` cannot inherit from another `record struct` or class. Only `record class` supports inheritance between records. Plan your type hierarchy accordingly."
        }
      ]
    }
  ],
  "interviewTakeaways": [
    "record class: heap object, value equality. record struct: copy, value equality.",
    "with is nondestructive copy, not mutation.",
    "Positional records generate constructor, init properties, Deconstruct, ToString."
  ],
  "commonPitfalls": [
    "Using a mutable record as a dictionary key.",
    "Expecting record inheritance to work on record struct."
  ],
  "relatedTopicIds": [
    "csharp-05-equality-and-hashing",
    "csharp-04-structs-vs-classes",
    "csharp-04-constructors-and-required",
    "csharp-11-with-immutability",
    "csharp-18-domain-dto-record"
  ],
  "jsTsCorrelations": [
    {
      "language": "javascript",
      "concept": "object spread",
      "note": "{ ...token, exp } copies enumerable properties shallowly. C# with copies the record's compiler-known members and preserves the runtime type in hierarchies.",
      "futureTopicSlug": "javascript/objects/spread"
    },
    {
      "language": "typescript",
      "concept": "readonly types and interfaces",
      "note": "TypeScript Readonly<T> is erased. C# record equality and with are runtime behaviors in generated members.",
      "futureTopicSlug": "typescript/types/readonly"
    }
  ],
  "officialSources": [
    {
      "title": "Records",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/record"
    },
    {
      "title": "Classes, structs, and records",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/object-oriented/"
    }
  ],
  "animationHint": "compare",
  "scenarioTag": "auth-token"
}
