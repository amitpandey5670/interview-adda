{
  "moduleId": "csharp-18-version-style-performance",
  "title": "Version and style in one screen",
  "rememberThis": [
    "LangVersion = syntax. TFM = available APIs. Two knobs, not one.",
    "Map C# 8–14 features to modules — interviewers want scenarios, not changelog.",
    "Domain has behavior; DTO record crosses boundaries; map once at the edge.",
    "Review radar: .Result, lock across await, ToList in loop, null! abuse."
  ],
  "bullets": [
    "Upgrade TFM before raising LangVersion or enabling nullable everywhere.",
    "Idiomatic: sealed services, async I/O, guard clauses, pattern matching.",
    "record struct for million-row CSV; record class for HTTP JSON shapes.",
    "Public API CI on libraries — internal by default."
  ],
  "rows": [
    { "label": "C# 8–14 anchor", "value": "NRT, records, patterns, IAsyncEnumerable, collections, spans" },
    { "label": "Central pins", "value": "Directory.Build.props: TFM, LangVersion, Nullable" },
    { "label": "DTO vs domain", "value": "DTO at HTTP/Redis edge; invariants inside domain class" },
    { "label": "First review flags", "value": "sync-over-async, missing CancellationToken, allocation traps" }
  ]
}
