{
  "moduleId": "csharp-18-version-style-performance",
  "title": "Version and style interview drills",
  "items": [
    {
      "question": "What is the difference between LangVersion and Target Framework Moniker?",
      "answer": "LangVersion tells Roslyn which C# syntax is legal — records, collection expressions, etc. TFM like net10.0 selects which Base Class Library APIs exist in reference assemblies. You can use older TFM with newer LangVersion only if you do not call APIs that do not exist. Upgrading TFM fixes API breaks; raising LangVersion adopts syntax. Pin both in Directory.Build.props for consistent CI.",
      "followUps": [
        "Can netstandard2.0 use C# 12 collection expressions?",
        "What does Nullable enable do on legacy code?"
      ],
      "sixtySeconds": "LangVersion syntax. TFM APIs. Upgrade TFM first. Pin in central props."
    },
    {
      "question": "Name three C# 9–11 features and where you used them in this curriculum.",
      "answer": "Records for immutable DTOs module 05. Nullable reference types for honest APIs module 07. Init-only properties and required members module 04. Pattern matching switch expressions module 11. IAsyncEnumerable streaming module 13. Collection expressions module 08. Tie each to a scenario — order DTO, auth branching, SQS paging.",
      "followUps": [
        "What did C# 14 add that touches span or properties?",
        "How is NRT different from TypeScript strictNullChecks?"
      ],
      "sixtySeconds": "Records NRT patterns async streams collections — each maps to a module and scenario."
    },
    {
      "question": "How do you split domain, DTO, and persistence models?",
      "answer": "Domain enforces rules — Order.Ship validates lines. DTO is wire shape — OrderDto for JSON and Redis without behavior. EF entity is persistence — map to domain or DTO at repository boundary, not in controllers. On hot CSV import use record struct rows before optional promotion to class DTO. Map once per request, not domain to DTO to view model chains.",
      "followUps": [
        "When is exposing EF entity from minimal API acceptable?",
        "How does Result<T> fit at the boundary?"
      ],
      "sixtySeconds": "Domain behavior inside. DTO at edge. Map once. struct row for bulk import."
    },
    {
      "question": "What do you flag first in a senior code review of an SQS consumer?",
      "answer": "Correctness and cancellation: is CancellationToken passed through receive and database calls? Then sync-over-async — .Result or .Wait. Then concurrency — shared Dictionary without Concurrent prefix. Then allocations — ToList or string concat per message. Then API surface if library code. SkiaSharp or streams without using. Name the fix from the relevant module.",
      "followUps": [
        "How do you handle poison messages?",
        "When is null-forgiving operator acceptable?"
      ],
      "sixtySeconds": "Cancellation first. No Result. Concurrent collections. No alloc loop. using native."
    }
  ]
}
