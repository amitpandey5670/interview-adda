{
  "id": "csharp-12-deferred-execution",
  "slug": "deferred-execution",
  "title": "Deferred execution and hot-path traps (plan vs run)",
  "moduleId": "csharp-12-linq",
  "order": 3,
  "hook": "Declaring a LINQ query stores a plan, not results. Each time you enumerate — foreach, Count, ToList — the plan re-runs against the current source. On a busy submit path, that double pass can return different data.",
  "sections": [
    {
      "heading": "What is this?",
      "blocks": [
        {
          "type": "prose",
          "text": "Deferred execution means a LINQ query does not run when you assign it to a variable. It runs when you enumerate it or call an immediate operator. Immediate operators — Count, Any, First, Max, Sum, ToList, ToArray — execute right away. Deferred operators that return IEnumerable typically split into two classes: streaming (Where, Select, SelectMany, Take) yield elements as they read, and buffering (OrderBy, GroupBy, Reverse) read the entire source before yielding the first element."
        },
        {
          "type": "glossary",
          "title": "Key terms",
          "entries": [
            {
              "term": "Deferred execution",
              "plainDefinition": "The query is a recipe stored in an IEnumerable. Work happens only when you enumerate or materialize.",
              "example": "var q = items.Where(x => x.Active); // no work yet",
              "longForm": "Deferred execution"
            },
            {
              "term": "Immediate execution",
              "plainDefinition": "Operators that return a scalar or a concrete collection run the query right away.",
              "example": "items.Where(x => x.Active).Count() // runs now",
              "longForm": "Immediate execution"
            },
            {
              "term": "Materialization",
              "plainDefinition": "Freezing a deferred query into a List or array with ToList or ToArray.",
              "example": "var snapshot = query.ToList(); // one pass, reusable",
              "longForm": "Materialization"
            },
            {
              "term": "Multiple enumeration",
              "plainDefinition": "Running the same IEnumerable plan more than once. Each pass re-executes all deferred operators.",
              "example": "var c = q.Count(); foreach (var x in q) { } // two passes",
              "longForm": "Multiple enumeration"
            }
          ]
        }
      ]
    },
    {
      "heading": "Why does it matter?",
      "blocks": [
        {
          "type": "prose",
          "text": "On a high-concurrency submit path, enumerating the same filtered query twice is two CPU passes and potentially two different snapshots if the underlying list is still growing. Count then foreach on the same IEnumerable is the classic production bug. Chaining two Where clauses is still one pass when you finally enumerate, but calling ToList in the middle creates a second pass on a copy. Captured variables (cutoff, tenantId) are read at enumeration time, not at query construction — a race if those locals change between passes."
        },
        {
          "type": "callout",
          "variant": "tip",
          "title": "Hot-path fix",
          "body": "Call ToList once to snapshot, then share the list. One pass, one consistent view of the data."
        }
      ]
    },
    {
      "heading": "How it works step by step",
      "blocks": [
        {
          "type": "steps",
          "title": "Plan versus execution",
          "items": [
            {
              "title": "Build the query",
              "body": "Assign a chain of Where/Select to a variable. No elements are read from the source."
            },
            {
              "title": "First enumeration",
              "body": "Count() or foreach walks the source through the filter. If the source changed, results reflect the new state."
            },
            {
              "title": "Second enumeration",
              "body": "Another foreach or Count re-runs the entire plan. Two passes, potentially different results."
            },
            {
              "title": "Snapshot with ToList",
              "body": "ToList runs the plan once and stores results. Subsequent operations use the frozen list."
            }
          ]
        },
        {
          "type": "diagram",
          "diagram": {
            "type": "mermaid",
            "title": "Deferred vs immediate operators",
            "source": "flowchart TD\n  Q[Query variable\\nstores plan] --> D{Enumerate?}\n  D -->|No| W[No work done]\n  D -->|foreach / ToList| E[Execute plan]\n  E --> S{Operator type}\n  S -->|Where Select Take| ST[Stream elements]\n  S -->|OrderBy GroupBy| BU[Buffer all first]\n  E --> M[Count First Max\\nImmediate scalar]"
          }
        }
      ]
    },
    {
      "heading": "Code walkthrough",
      "blocks": [
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Double enumeration on a live submit buffer",
            "code": "var pending = new List<Attempt>(); // mutated by other tasks\n\nIEnumerable<Attempt> late =\n    pending.Where(a => a.SubmittedAt > cutoff);\n\nvar count = late.Count();          // pass 1 — walks the filter\nforeach (var a in late)            // pass 2 — different membership!\n{\n    Report(a);\n}\n\nvar snapshot = late.ToList();      // one pass, then reuse snapshot",
            "explanation": "Count and foreach each run Where independently. Inserts between them change the report. ToList once is the hot-path fix."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Captured variable read at enumeration time",
            "code": "int minScore = 50;\nvar query = attempts.Where(a => a.Score >= minScore);\n\nminScore = 80; // changed after query built\n\nforeach (var a in query)\n{\n    // Uses minScore = 80, not 50 — deferred by design\n    Console.WriteLine(a.Id);\n}",
            "explanation": "The lambda closes over minScore by reference. The value at enumeration time is what matters. Snapshot locals if you need a fixed threshold."
          }
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Loop variable capture in deferred queries",
            "code": "IEnumerable<IEnumerable<Attempt>> queries = [];\nfor (var tenant = 0; tenant < tenantCount; tenant++)\n{\n    var id = tenant; // snapshot — required!\n    queries = queries.Append(attempts.Where(a => a.TenantId == id));\n}\n\n// Without 'var id = tenant', every query would see the final tenant value.",
            "explanation": "Closing over a for-loop iterator variable captures the variable, not the value. Copy to a local inside the loop body before using it in a lambda."
          }
        }
      ]
    },
    {
      "heading": "Compare with JavaScript and TypeScript",
      "blocks": [
        {
          "type": "comparisonTable",
          "title": "Deferred LINQ vs eager Array methods",
          "headers": [
            "Aspect",
            "C# LINQ (deferred)",
            "JavaScript Array"
          ],
          "rows": [
            [
              "When work runs",
              "On enumeration or ToList/Count",
              "Immediately on filter/map"
            ],
            [
              "Re-running",
              "Each foreach re-executes the plan",
              "Each filter creates a new array once"
            ],
            [
              "Lazy analogue",
              "IEnumerable iterator chain",
              "Generator functions (function*)"
            ],
            [
              "Snapshot",
              "ToList() or ToArray()",
              "Spread [...arr] or Array.from"
            ],
            [
              "Double-iterate trap",
              "Same IEnumerable, two passes",
              "Spreading a generator twice re-runs it"
            ]
          ]
        }
      ]
    },
    {
      "heading": "Common mistakes",
      "blocks": [
        {
          "type": "callout",
          "variant": "warning",
          "title": "Pitfalls to avoid",
          "body": "Calling Count then foreach on the same IEnumerable. Returning a query over a using-disposed enumerator (yield after dispose). Putting expensive Select lambdas in a query that reporting enumerates three times. Assuming OrderBy streams — it buffers the entire source first."
        }
      ]
    },
    {
      "heading": "Senior interview depth",
      "blocks": [
        {
          "type": "prose",
          "text": "Deferred execution means each operator wraps the previous in a new **iterator** — every Where/Select allocates a delegate and an enumerator object on the heap (struct enumerators box when the API returns IEnumerable<T>). Count then foreach on the same plan is two full passes; on List<T> use **TryGetNonEnumeratedCount** for the first pass when you only need count, or **ToList()** once to snapshot. Captured locals are read at enumeration time, not query construction — a race on mutable cutoff flags. OrderBy/GroupBy defer but buffer entirely before first yield. For CPU-bound in-memory aggregation on large snapshots, **Parallel LINQ (PLINQ)** via AsParallel() is an opt-in follow-up — never the default answer for deferred traps. How to prove it in production: dotnet-counters or dotMemory showing double enumeration on submit-path reporting; log enumeration count per request."
        },
        {
          "type": "comparisonTable",
          "title": "Tradeoffs at senior level",
          "headers": ["Pattern", "Pass count", "Fix"],
          "rows": [
            ["Count then foreach (deferred)", "Two full walks", "ToList once or TryGetNonEnumeratedCount + single foreach"],
            ["Multiple report sections same query", "N passes", "Materialize snapshot at boundary"],
            ["Live List while querying", "Non-deterministic between passes", "ToList before parallel readers"],
            ["OrderBy in chain", "Buffers all before yield", "Do not assume streaming"],
            ["AsParallel on deferred chain", "Partitions at enumeration", "Measure; overhead on small seqs"]
          ]
        },
        {
          "type": "snippet",
          "snippet": {
            "language": "csharp",
            "label": "Iterator allocations in a deferred chain",
            "code": "IEnumerable<int> chain =\n    source.Where(x => x > 0)   // WhereIterator + Func\n        .Select(x => x * 2)    // SelectIterator + Func\n        .Take(10);              // TakeIterator\n\n// Each operator adds an iterator object; lambdas are delegate allocations.\n// Fix on hot path: one foreach loop, or for-loop on List/Span, or ToList once.\n\nReadOnlySpan<int> span = CollectionsMarshal.AsSpan(list);\nfor (int i = 0; i < span.Length; i++)\n{\n    if (span[i] > 0) { /* ... */ }\n}",
            "explanation": "Senior interviews connect deferred execution to allocation cost, not just correctness. TryGetNonEnumeratedCount and materialization are the LINQ-side fixes; Span/for-loop is the escape hatch."
          }
        },
        {
          "type": "callout",
          "variant": "warning",
          "title": "What gets you rejected in interviews",
          "body": "Saying deferred execution is always free until ToList — each enumeration runs the full chain and allocates iterators. Ignoring Count then foreach as two passes. Recommending PLINQ for every LINQ query — it is for measured CPU-bound in-memory work. Strong answer: plan vs run, snapshot with ToList, TryGetNonEnumeratedCount when ICollection, iterator allocations on hot paths."
        }
      ]
    }
  ],
  "interviewTakeaways": [
    "Declaration is a plan; each enumeration re-runs the chain and allocates iterators.",
    "Count/ToList/First are immediate; Where/Select defer but add iterator layers per operator.",
    "TryGetNonEnumeratedCount avoids a full walk for Count on ICollection-backed sources.",
    "60s: Deferred means plan not results. Each foreach re-runs and allocates iterators. ToList snapshots once. Count then foreach is two passes. TryGetNonEnumeratedCount on lists. PLINQ AsParallel only when measured CPU-bound.",
    "Follow-up: what allocates on a three-operator Where-Select-Take chain?"
  ],
  "commonPitfalls": [
    "Calling Count then foreach on the same IEnumerable — two passes; rejected on hot submit paths.",
    "Strong answer: ToList once at the boundary, or TryGetNonEnumeratedCount when the source exposes Count.",
    "Returning a query over a using-disposed enumerator — yield after dispose throws or returns empty.",
    "Assuming OrderBy streams — it buffers the entire source before the first element."
  ],
  "relatedTopicIds": [
    "csharp-12-linq-to-objects",
    "csharp-12-ienumerable-vs-iqueryable",
    "csharp-10-lambdas-and-closures",
    "csharp-08-iterators-yield",
    "csharp-14-allocation-traps"
  ],
  "jsTsCorrelations": [
    {
      "language": "javascript",
      "concept": "Eager Array methods versus iterators",
      "note": "array.filter is eager. JS iterators and generator pipelines are the deferred analogue. LINQ to Objects is closer to a generator chain than to Array.prototype.",
      "futureTopicSlug": "javascript/iterators/lazy-pipelines"
    },
    {
      "language": "javascript",
      "concept": "Lazy iterables",
      "note": "A JS generator is lazy like IEnumerable until you spread it. Spreading twice re-runs the generator if you still have it.",
      "futureTopicSlug": "javascript/language/iteration-protocols"
    },
    {
      "language": "typescript",
      "concept": "generator double consumption",
      "note": "TypeScript generators defer like IEnumerable but have no TryGetNonEnumeratedCount — materialize with Array.from or spread before counting and iterating.",
      "futureTopicSlug": "typescript/iterators/generators"
    }
  ],
  "officialSources": [
    {
      "title": "Introduction to LINQ queries — deferred execution",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/linq/get-started/introduction-to-linq-queries"
    }
  ],
  "animationHint": "timeline",
  "scenarioTag": "high-concurrency"
}
