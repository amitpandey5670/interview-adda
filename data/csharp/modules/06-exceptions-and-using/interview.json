{
  "moduleId": "csharp-06-exceptions-and-using",
  "title": "Exceptions and using — interview",
  "items": [
    {
      "question": "What can you throw in C# versus JavaScript?",
      "answer": "C# throw requires a System.Exception (or derived type). JavaScript throw accepts any value — strings, numbers, plain objects — which makes catch typing guesswork. On the Common Language Runtime (CLR) the thrown object carries Message, StackTrace, InnerException, and sometimes HResult. Uncaught exceptions kill the thread; in a hosted process they can take down the application.",
      "followUps": [
        "What happens if a static constructor throws?",
        "Where do unobserved Task exceptions show up?"
      ],
      "sixtySeconds": "Throw Exception types only. JS can throw anything. Unobserved tasks are module 13."
    },
    {
      "question": "When do you catch in a queue worker versus letting it bubble?",
      "answer": "Catch at the message boundary when you can log, retry with backoff, or send to a dead-letter queue (DLQ) and complete the receive. Catching Exception and swallowing leaves poison messages looping or causes silent data loss. Framework and host-level handlers exist for unhandled exceptions; bubbling is correct when you cannot restore state. Use when filters to catch transient faults without hiding bugs.",
      "followUps": [
        "Where does finally run relative to a rethrow?",
        "Should you catch OperationCanceledException?"
      ],
      "sixtySeconds": "Worker boundary: recover, retry, DLQ. Unknown failures bubble. Do not empty-catch Exception."
    },
    {
      "question": "throw versus throw ex versus wrapping?",
      "answer": "throw; rethrows the current exception and keeps the original StackTrace. throw ex; starts a new throw site and you lose the original frames — the classic bug. Wrapping is new OuterException(message, inner) when the caller needs a domain-level type plus the root cause. Never wrap just to log; log and throw; or wrap once at a boundary.",
      "followUps": [
        "What does ExceptionDispatchInfo.Throw do?",
        "Why is catch (Exception) { throw; } sometimes still wrong?"
      ],
      "sixtySeconds": "throw; keeps the stack. throw ex; destroys it. Wrap with InnerException at boundaries."
    },
    {
      "question": "How do you dispose a SkiaSharp SKSurface correctly?",
      "answer": "SKSurface, SKCanvas, SKBitmap, and SKPaint hold native resources. Implement or consume IDisposable and wrap with using (or await using for async equivalents). The compiler emits try/finally so Dispose runs on exceptions and returns. Do not wait for a finalizer — that is module 14 and too late for native memory under high-concurrency image work.",
      "followUps": [
        "using declaration versus using statement?",
        "What is IAsyncDisposable for?"
      ],
      "sixtySeconds": "using owns IDisposable. SkiaSharp is native. Finalizers later. await using for async disposal."
    },
    {
      "question": "Explain try, catch, finally, and when filters.",
      "answer": "try marks risky work. catch handles a compatible exception type — most derived first. finally always runs for that try, even on rethrow. when adds a filter: catch (HttpRequestException ex) when (ex.StatusCode == TooManyRequests) handles throttling without catching other HTTP failures. Filters should be fast and side-effect free.",
      "followUps": [
        "Does finally run before return inside try?",
        "What is a bare catch without a type?"
      ],
      "sixtySeconds": "try/catch/finally plus when filters. finally always runs. Most-derived catch first."
    }
  ]
}
