{
  "moduleId": "csharp-09-generics",
  "title": "Generics — quick overview",
  "rememberThis": [
    "List<T> is type-safe and does not box int. ArrayList stores object and boxes value types.",
    "C# generics are reified; generic sharing applies to reference-type type arguments at JIT level.",
    "Open generic typeof(List<>) versus closed List<string>; statics are per closed type.",
    "out T covariant, in T contravariant; IList<T> invariant. Result/TryGet beats T? Get for Redis.",
    "Constraints: struct and new() cannot combine; allows ref struct last."
  ],
  "bullets": [
    "Open generic: List<T>. Closed generic: List<string>.",
    "new() cannot combine with struct or unmanaged and cannot construct types with required members.",
    "allows ref struct is an anti-constraint so T can be Span-like (see module 17).",
    "TypeScript variance is structural and historically bivariant on callbacks; C# variance is declared with out/in.",
    "Dictionary<TKey, TValue> is the everyday two-parameter generic."
  ],
  "rows": [
    {
      "label": "class / class?",
      "value": "Reference type; class is non-nullable in a nullable reference type context"
    },
    {
      "label": "struct",
      "value": "Non-nullable value type; implies parameterless constructor; not nullable T?"
    },
    {
      "label": "notnull",
      "value": "Non-nullable T; violation is a warning"
    },
    {
      "label": "unmanaged",
      "value": "Blittable value type; implies struct"
    },
    {
      "label": "Enum / Delegate",
      "value": "Base-class constraints — legal since modern C#"
    },
    {
      "label": "allows ref struct",
      "value": "T may be a ref struct; must follow ref-safety rules"
    }
  ]
}
