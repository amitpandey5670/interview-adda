{
  "moduleId": "csharp-16-assemblies-attributes-reflection",
  "title": "Assemblies and metadata in one screen",
  "rememberThis": [
    "Assembly = IL + metadata + manifest — AssemblyLoadContext for plugin isolation.",
    "DynamicallyAccessedMembers roots types for trim and Native AOT when reflection remains.",
    "Source generators emit C# at compile time — JsonSerializerContext replaces hot-path reflection.",
    "Public API CI blocks accidental breaking changes — internal by default."
  ],
  "bullets": [
    "Collectible ALC unloads plugins when no references remain — Default ALC is not unloadable.",
    "Compiler honors Obsolete, Conditional, nullable; frameworks read validation and JSON attributes.",
    "Reflection at startup OK; per-message Invoke fails under SQS load — use generators or switches.",
    "PublicApiAnalyzers and ApiCompat diff exported surface on every pull request."
  ],
  "rows": [
    { "label": "Deployment unit", "value": ".dll assembly with metadata — ALC for plugin load/unload" },
    { "label": "Trim / AOT", "value": "DynamicallyAccessedMembers roots or source generators" },
    { "label": "Replace reflection", "value": "IIncrementalGenerator, JsonSerializerContext, typed switch" },
    { "label": "CI guard", "value": "PublicAPI.Shipped.txt baseline or ApiCompat against prior package" }
  ]
}
