{
  "moduleId": "javascript-06-errors-and-debugging",
  "title": "Errors and debugging — one screen",
  "rememberThis": [
    "Throw new Error (or subclass) — not strings — so you get message and stack.",
    "try/catch only catches promise rejections when you await inside try. Bare asyncFn() needs .catch.",
    "C# typed catch and when filters become one catch with instanceof and if in JavaScript."
  ],
  "bullets": [
    "finally always runs for that try — cleanup leases, temp files, and connections.",
    "Unhandled rejections: process.on('unhandledRejection') in Node; window unhandledrejection in browser.",
    "Upload source maps to your error tracker — stacks in bundles point to line 1 without them."
  ],
  "rows": [
    { "label": "throw", "value": "Any value; prefer Error subclass" },
    { "label": "catch", "value": "Single block — inspect with instanceof" },
    { "label": "finally", "value": "Always runs (cleanup)" },
    { "label": "cause", "value": "Wrap root error — like InnerException" },
    { "label": "await in try", "value": "Required for async errors to hit catch" },
    { "label": "unhandledRejection", "value": "Promise rejected with no handler" },
    { "label": "err.stack", "value": "Call frames — log in production" },
    { "label": "source map", "value": "Bundle → original TypeScript lines" }
  ]
}
